<h1>Items</h1>
<ul>
  <% @items.each do |item| %>
    <li>
      <%= link_to item.title, item_path(item) %>
      <%= link_to "book", item_bookings_path(item), method: :post %>
    </li>
  <% end %>
</ul>

<%= link_to "add a new item", new_item_path %>
