<%= simple_form_for [@item] do |f| %>
      <%= f.input :title %>
      <%= f.input :description %>
      <%= f.input :price %>
      <%= f.button :submit, class: 'button' %>
    <% end %>

<%= link_to "homepage", items_path %>
<%= link_to "back to item", item_path(@item) %>
