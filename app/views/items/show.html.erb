<h1><%= @item.title %></h1>
<p><%= @item.description %></p>
<p><%= @item.price %></p>

<%= link_to "homepage", items_path %>
<%= link_to "edit", edit_item_path(@item) %>
<%= link_to "Delete", item_path(@item), method: :delete, data: { confirm: "Are you sure you want to delete this item?" }, class: 'button' %>
